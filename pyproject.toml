[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta:__legacy__"

[tool.pylint.messages_control]
disable = ["C0115", "R0903"]  # missing-class-docstring, too-few-public-methods